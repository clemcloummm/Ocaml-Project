(library
 (name graph_project)
 (modules graph tools gfile ford_fulk)
 (wrapped false))

(executable
   (name ftest)
 ;; (libraries )

 ;; Disable some warnings
 ;; (flags (:standard -w -6))

 ;; Link to executable in main dir
  (promote (until-clean))
  (modules ftest)
  (libraries graph_project)
)

(executable
  (name tests)
  (modules tests)
  (libraries graph_project)

)

